# Database Directory
This directory contains the database connection setup, Drizzle ORM schema definitions, and migration scripts.
- `schema.ts`: Defines the database tables and relationships.
- `index.ts`: Initializes the Drizzle database instance.
